{{- if .Values.langflow.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bomarag.fullname" . }}-langflow
  namespace: {{ include "bomarag.namespace" . }}
  labels:
    {{- include "bomarag.langflow.labels" . | nindent 4 }}
spec:
  type: {{ .Values.langflow.service.type }}
  ports:
    - port: {{ .Values.langflow.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bomarag.langflow.selectorLabels" . | nindent 4 }}
{{- end }}
