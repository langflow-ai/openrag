# Load nvm so npx is available regardless of how the hook is launched (e.g. GUI Git clients).
export NVM_DIR="${NVM_DIR:-$HOME/.nvm}"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
cd frontend && npx lint-staged
