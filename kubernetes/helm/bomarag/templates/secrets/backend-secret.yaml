{{- if .Values.backend.enabled }}
{{- $secretName := printf "%s-backend" (include "bomarag.fullname" .) }}
{{- $secretNamespace := include "bomarag.namespace" . }}
{{- $existingSecret := lookup "v1" "Secret" $secretNamespace $secretName }}
{{- $sessionSecret := "" }}
{{- if $existingSecret }}
{{- $sessionSecret = index $existingSecret.data "session-secret" | b64dec }}
{{- else }}
{{- $sessionSecret = include "bomarag.backend.sessionSecret" . }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ $secretNamespace }}
  labels:
    {{- include "bomarag.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  session-secret: {{ $sessionSecret | b64enc }}
{{- end }}
