{{- if .Values.postgres.enabled }}
{{- $secretName := printf "%s-postgres" (include "bomarag.fullname" .) }}
{{- $secretNamespace := include "bomarag.namespace" . }}
{{- $existingSecret := lookup "v1" "Secret" $secretNamespace $secretName }}
{{- $password := "" }}
{{- if $existingSecret }}
{{- $password = index $existingSecret.data "postgres-password" | b64dec }}
{{- else }}
{{- $password = include "bomarag.postgres.password" . }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ $secretNamespace }}
  labels:
    {{- include "bomarag.labels" . | nindent 4 }}
  annotations:
    helm.sh/resource-policy: keep
type: Opaque
data:
  postgres-password: {{ $password | b64enc }}
{{- end }}

# Made with Bob
