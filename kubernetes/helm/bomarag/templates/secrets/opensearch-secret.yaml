{{- if .Values.global.opensearch.password }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bomarag.fullname" . }}-opensearch
  namespace: {{ include "bomarag.namespace" . }}
  labels:
    {{- include "bomarag.labels" . | nindent 4 }}
type: Opaque
stringData:
  password: {{ .Values.global.opensearch.password | quote }}
  username: {{ .Values.global.opensearch.username | quote }}
{{- end }}
