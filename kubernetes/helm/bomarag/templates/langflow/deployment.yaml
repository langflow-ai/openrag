{{- if .Values.langflow.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "bomarag.fullname" . }}-langflow
  namespace: {{ include "bomarag.namespace" . }}
  labels:
    {{- include "bomarag.langflow.labels" . | nindent 4 }}
spec:
  replicas: 1  # Always 1 for SQLite-based Langflow
  strategy:
    type: Recreate  # Required for RWO PVC
  selector:
    matchLabels:
      {{- include "bomarag.langflow.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "bomarag.langflow.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/secret: {{ include (print $.Template.BasePath "/secrets/langflow-secret.yaml") . | sha256sum }}
        checksum/config: {{ include (print $.Template.BasePath "/configmaps/flow-ids-configmap.yaml") . | sha256sum }}
        checksum/dotenv: {{ include (print $.Template.BasePath "/langflow/langflow-dotenv.yaml") . | sha256sum }}
    spec:
      serviceAccountName: {{ include "bomarag.serviceAccountName" . }}

      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}


      containers:
        - name: langflow
          image: "{{ .Values.langflow.image.repository }}:{{ .Values.langflow.image.tag | default .Values.global.imageTag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}

          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}

          ports:
            - name: http
              containerPort: 7860
              protocol: TCP

          command: ["langflow", "run", "--env-file", "/app/.env"]

          volumeMounts:
            - name: bomarag-langflow-dotenv
              mountPath: /app/.env
              subPath: .env
              readOnly: true
            - name: langflow-data
              mountPath: {{ .Values.langflow.persistence.mountPath }}
            - name: shared
              mountPath: {{ .Values.backend.persistence.shared.mountPath | quote }}
              readOnly: true

          resources:
            {{- toYaml .Values.langflow.resources | nindent 12 }}

          {{- if .Values.langflow.livenessProbe.enabled }}
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.langflow.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.langflow.livenessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.langflow.livenessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.langflow.livenessProbe.failureThreshold }}
          {{- end }}

          {{- if .Values.langflow.readinessProbe.enabled }}
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: {{ .Values.langflow.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.langflow.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.langflow.readinessProbe.timeoutSeconds }}
            failureThreshold: {{ .Values.langflow.readinessProbe.failureThreshold }}
          {{- end }}

      volumes:
        - name: bomarag-langflow-dotenv
          secret:
            secretName: {{ include "bomarag.fullname" . }}-langflow-dotenv
            defaultMode: 0400
            items:
              - key: .env
                path: .env
        - name: langflow-data
          {{- if .Values.langflow.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "bomarag.fullname" . }}-langflow
          {{- else }}
          emptyDir: {}
          {{- end }}
        {{- if .Values.backend.persistence.shared.enabled }}
        - name: shared
          persistentVolumeClaim:
            claimName: {{ include "bomarag.fullname" . }}-shared
        {{- end }}

      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}

      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
