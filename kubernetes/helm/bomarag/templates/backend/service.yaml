{{- if .Values.backend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bomarag.fullname" . }}-backend
  namespace: {{ include "bomarag.namespace" . }}
  labels:
    {{- include "bomarag.backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    {{- if .Values.backend.ingestRouter.enabled }}
    - port: {{ .Values.backend.ingestRouter.port }}
      targetPort: ingest-router
      protocol: TCP
      name: ingest-router
    {{- end }}
  selector:
    {{- include "bomarag.backend.selectorLabels" . | nindent 4 }}
{{- end }}
