{{- if .Values.langflow.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bomarag.fullname" . }}-langflow
  namespace: {{ include "bomarag.namespace" . }}
  labels:
    {{- include "bomarag.labels" . | nindent 4 }}
type: Opaque
stringData:
  secret-key: {{ .Values.langflow.auth.secretKey | default (randAlphaNum 32) | quote }}
  superuser: {{ .Values.langflow.auth.superuser | quote }}
  superuser-password: {{ .Values.langflow.auth.superuserPassword | default (randAlphaNum 16) | quote }}
  langfuse-public-key: {{ .Values.langflow.langfuse.publicKey | quote }}
  langfuse-secret-key: {{ .Values.langflow.langfuse.secretKey | quote }}
{{- end }}
