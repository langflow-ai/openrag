{{- if .Values.global.aws.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bomarag.fullname" . }}-aws
  namespace: {{ include "bomarag.namespace" . }}
  labels:
    {{- include "bomarag.labels" . | nindent 4 }}
type: Opaque
stringData:
  access-key-id: {{ .Values.global.aws.accessKeyId | quote }}
  secret-access-key: {{ .Values.global.aws.secretAccessKey | quote }}
{{- end }}
