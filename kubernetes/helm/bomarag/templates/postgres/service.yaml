{{- if .Values.postgres.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "bomarag.fullname" . }}-postgres
  namespace: {{ include "bomarag.namespace" . }}
  labels:
    {{- include "bomarag.postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.postgres.service.type }}
  ports:
    - port: {{ .Values.postgres.service.port }}
      targetPort: postgres
      protocol: TCP
      name: postgres
  selector:
    {{- include "bomarag.postgres.selectorLabels" . | nindent 4 }}
{{- end }}

# Made with Bob
