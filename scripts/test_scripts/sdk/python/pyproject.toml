[project]
name = "bomarag-sdk-smoke-tests"
version = "0.1.0"
description = "Smoke-test suite for the BomaRAG Python SDK against a remote (IBM SaaS) deployment"
requires-python = ">=3.10"
dependencies = ["bomarag-sdk>=0.3.1"]

[tool.uv]
package = false
