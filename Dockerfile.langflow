# syntax=docker/dockerfile:1.4
# UBI 9 Python 3.12 base image for enterprise compliance
FROM registry.access.redhat.com/ubi9/python-312:9.5

# Switch to root to install system packages
USER 0

# Install system dependencies
RUN dnf install -y --allowerasing \
    git \
    gcc \
    gcc-c++ \
    make \
    ca-certificates \
    && dnf clean all

# Install SQLite >= 3.35.0 (UBI9 has 3.34.1) from source, since there are no built wheels available for Python 3.12
# This is required by chromadb which is a dependency of langflow
RUN curl -L https://www.sqlite.org/2024/sqlite-autoconf-3460000.tar.gz | tar -xz \
    && cd sqlite-autoconf-3460000 \
    && ./configure --prefix=/usr --libdir=/usr/lib64 \
    && make -j$(nproc) install \
    && cd .. \
    && rm -rf sqlite-autoconf-3460000

# Install Langflow and dependencies
RUN pip install --no-cache-dir \
    langflow==1.9.6rc0 \
    uv

# Create application directory and langflow data directory
WORKDIR /app
RUN mkdir -p /app/data

# Copy flows
COPY flows/ /app/flows/

# Copy the entrypoint script
COPY --chmod=755 scripts/langflow-entrypoint.py /usr/local/bin/langflow-entrypoint

# Pre-create all writable dirs with OpenShift-safe permissions
RUN mkdir -p /app/data/.mem0 \
             /app/data/.cache \
             /app/data/.gunicorn \
    && chgrp -R 0 /app/data \
    && chmod -R g=u /app/data

# Bake env vars into image — covers cases where entrypoint is bypassed
ENV HOME=/app/data
ENV XDG_CACHE_HOME=/app/data/.cache
ENV MEM0_DIR=/app/data/.mem0
ENV LANGFLOW_CONFIG_DIR=/app/data
ENV GUNICORN_CMD_ARGS="--worker-tmp-dir /app/data/.gunicorn"
ENV LANGFLOW_ALEMBIC_LOG_TO_STDOUT=true

# Set proper ownership for the app directory with OpenShift compatibility
# Use root group (GID 0) with group-write permissions for OpenShift arbitrary UIDs
RUN if ! getent group 1000 >/dev/null; then \
        groupadd --gid 1000 langflow; \
    fi \
    && if ! getent passwd 1000 >/dev/null; then \
        useradd --uid 1000 --gid 1000 --no-create-home langflow; \
    fi \
    && chown -R root:0 /app \
    && chmod -R g+rwX /app

# NOTE: Container runs as root initially so entrypoint can fix permissions
# The entrypoint script drops to UID 1000 before executing the main process

EXPOSE 7860

ENTRYPOINT ["langflow-entrypoint"]
CMD ["langflow", "run", "--host", "0.0.0.0", "--port", "7860"]